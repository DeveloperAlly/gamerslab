name: Geo Monitor

on:
  schedule:
    - cron: '*/5 * * * *'   # every 5 minutes
  workflow_dispatch:
    inputs:
      mode:
        description: 'standard or surge'
        default: 'standard'
        required: true
      regions:
        description: 'comma-separated regions (informational only)'
        default: 'us-east,eu-west,ap-southeast,sa-east,me-south,af-south'
        required: false

jobs:
  monitor:
    strategy:
      fail-fast: false
      matrix:
        include:
          - region: us-east
            runner: ubuntu-latest
          - region: eu-west
            runner: ubuntu-latest
          - region: ap-southeast
            runner: ubuntu-latest
          - region: sa-east
            runner: ubuntu-latest
          - region: me-south
            runner: ubuntu-latest
          - region: af-south
            runner: ubuntu-latest

    runs-on: ${{ matrix.runner }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: Setup Node.js
        uses: actions/setup-node@v4.4.0
        with:
          node-version: '24'

      - name: Install Playwright
        run: |
          npm init -y
          npm install playwright@latest
          npx playwright install chromium --with-deps

      - name: Run Playwright monitor
        env:
          REGION: ${{ matrix.region }}
          MODE: ${{ github.event.inputs.mode || 'standard' }}
          TARGET_URL: ${{ secrets.TARGET_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          node .github/scripts/monitor.js

      - name: Push to Supabase
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          REGION: ${{ matrix.region }}
          MODE: ${{ github.event.inputs.mode || 'standard' }}
        run: |
          python3 - <<'EOF'
          import json, urllib.request, os, time

          raw = os.environ.get('MONITOR_RESULT', '{}')
          try:
            data = json.loads(raw)
          except Exception:
            data = {}

          try:
            runner_ip = urllib.request.urlopen('https://api.ipify.org', timeout=3).read().decode()
          except Exception:
            runner_ip = None

          payload = json.dumps({
            "region":               os.environ['REGION'],
            "status":               1 if data.get('ok') else 0,
            "ttfb_ms":              data.get('ttfb_ms'),
            "content_language":     data.get('content_language'),
            "accept_language_sent": data.get('accept_language_sent'),
            "cf_colo":              data.get('cf_colo'),
            "runner_ip":            runner_ip,
            "mode":                 os.environ.get('MODE', 'standard'),
            "checked_at":           time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime()),
            "page_title":           data.get('page_title'),
            "game_iframe_loaded":   data.get('game_iframe_loaded'),
            "js_errors":            json.dumps(data.get('js_errors', [])),
            "page_blocked":         data.get('page_blocked'),
            "render_error":         data.get('render_error'),
            "referrer_used":        data.get('referrer_used'),
          }).encode()

          req = urllib.request.Request(
            f"{os.environ['SUPABASE_URL']}/rest/v1/monitor_results",
            data=payload,
            headers={
              'apikey':         os.environ['SUPABASE_KEY'],
              'Authorization':  f"Bearer {os.environ['SUPABASE_KEY']}",
              'Content-Type':   'application/json',
              'Prefer':         'return=minimal'
            },
            method='POST'
          )
          urllib.request.urlopen(req)
          print(f"Pushed: region={os.environ['REGION']} ok={data.get('ok')} ttfb={data.get('ttfb_ms')}ms iframe={data.get('game_iframe_loaded')} referrer={data.get('referrer_used')}")
          EOF

      - name: Surge parallel checks
        if: ${{ github.event.inputs.mode == 'surge' }}
        env:
          REGION: ${{ matrix.region }}
          TARGET_URL: ${{ secrets.TARGET_URL }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: |
          node .github/scripts/surge.js
